<!DOCTYPE html>
<html lang="ar" dir="rtl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>نسيت كلمة المرور - التطبيق الآمن</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="auth-container">
        <h2>استعادة كلمة المرور</h2>
        <p style="color: #ccc; text-align: center; margin-bottom: 20px;">أدخل بريدك الإلكتروني لاستلام رابط إعادة تعيين
            كلمة المرور.</p>
        <% if(error) { %>
            <div class="alert error" style="color: red; margin-bottom: 10px; text-align: center;">
                <%= error %>
            </div>
            <% } %>
                <% if(msg) { %>
                    <div class="alert success" style="color: green; margin-bottom: 10px; text-align: center;">
                        <%= msg %>
                    </div>
                    <% } %>
                        <form action="/auth/forgot-password" method="POST">
                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                            <div class="form-group">
                                <label for="email">البريد الإلكتروني</label>
                                <input type="email" id="email" name="email" required>
                            </div>
                            <button type="submit" class="btn-gold">إرسال الرابط</button>
                        </form>
                        <p style="text-align: center; margin-top: 15px; color: #ccc;">
                            <a href="/auth/login">رجوع لتسجيل الدخول</a>
                        </p>
    </div>
</body>

</html>