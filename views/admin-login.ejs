<!DOCTYPE html>
<html lang="ar" dir="rtl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>لوحة التحكم - تسجيل الدخول</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        .admin-login-box {
            max-width: 380px;
            margin: 100px auto;
            padding: 30px;
            background: linear-gradient(145deg, #1a1a1a, #111);
            border: 1px solid #333;
            border-radius: 15px;
            text-align: center;
        }

        .admin-login-box h2 {
            color: var(--primary-gold);
            margin-bottom: 20px;
        }

        .admin-login-box input {
            width: 100%;
            padding: 12px;
            border-radius: 10px;
            border: 1px solid #333;
            background: #222;
            color: #fff;
            font-size: 1.1rem;
            text-align: center;
            margin-bottom: 15px;
            letter-spacing: 5px;
            font-weight: bold;
        }

        .error-msg {
            color: #ff4d4d;
            margin-bottom: 10px;
            font-weight: bold;
        }
    </style>
</head>

<body>
    <div class="admin-login-box">
        <i class="fas fa-shield-alt fa-3x" style="color: var(--primary-gold); margin-bottom: 15px;"></i>
        <h2>لوحة التحكم</h2>
        <p style="color: #888; margin-bottom: 20px;">أدخل رمز الدخول</p>
        <% if (error) { %>
            <p class="error-msg">
                <%= error %>
            </p>
            <% } %>
                <form method="POST" action="/admin/login">
                    <input type="password" name="code" placeholder="••••" required autofocus>
                    <button type="submit" class="btn-gold" style="width: 100%;">دخول</button>
                </form>
    </div>
</body>

</html>