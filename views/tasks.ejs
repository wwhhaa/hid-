<!DOCTYPE html>
<html lang="ar" dir="rtl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>المهام</title>
    <link rel="stylesheet" href="/css/style.css?v=2">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        .progress-container {
            width: 100%;
            background-color: #333;
            border-radius: 10px;
            margin: 15px 0;
            height: 20px;
            overflow: hidden;
            position: relative;
        }

        .progress-bar {
            height: 100%;
            background: linear-gradient(90deg, #ff4d4d, #cc0000);
            width: 0%;
            transition: width 0.5s ease;
        }

        .task-card {
            background: #1a1a1a;
            border: 1px solid #333;
            border-radius: 15px;
            padding: 20px;
            margin-bottom: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
        }
    </style>
</head>

<body>
    <header>
        <div style="font-weight: 800; color: #fff;">المهام اليومية</div>
        <div class="points-display">
            <span id="user-points">
                <%= user.points %>
            </span> <i class="fas fa-coins" style="color: #FFD700;"></i>
        </div>
    </header>

    <div class="container" style="padding-bottom: 100px;">

        <!-- Red Card Tracker -->
        <div class="stat-card" style="border: 1px solid #ff4d4d;">
            <h3 style="color: #ff4d4d; margin-bottom: 15px;">
                <i class="fas fa-ticket-alt"></i> بطاقة العجلة الحمراء
            </h3>
            <p style="color: #aaa; font-size: 0.9rem; margin-bottom: 10px;">
                شاهد 7 إعلانات للحصول على بطاقة واحدة. يمكنك تدوير العجلة فقط باستخدام هذه البطاقة.
            </p>

            <div style="display: flex; justify-content: space-between; font-weight: bold; margin-bottom: 5px;">
                <span style="color: #fff;">التقدم: <span id="ads-count">
                        <%= user.ads_watched %>
                    </span>/7</span>
                <span style="color: #ff4d4d;">لديك: <span id="cards-count">
                        <%= user.red_cards %>
                    </span> بطاقة</span>
            </div>

            <div class="progress-container">
                <div class="progress-bar" id="progress-bar" style="width: <%= (user.ads_watched / 7) * 100 %>%;"></div>
            </div>

            <p style="text-align: right; color: #888; font-size: 0.8rem; margin-bottom: 15px;">
                <% let maxCards=2; if (user.vip_level===1) maxCards=3; else if (user.vip_level===2) maxCards=4; else if
                    (user.vip_level===3) maxCards=6; %>
                    الحد اليومي: <span id="daily-limit">
                        <%= user.cards_earned_today %>
                    </span>/<%= maxCards %> بطاقة
            </p>

            <button id="watch-ad-btn" class="btn-gold"
                style="background: linear-gradient(135deg, #ff4d4d, #a00); color: white; box-shadow: 0 5px 15px rgba(255, 77, 77, 0.3);">
                <i class="fas fa-play-circle"></i> مشاهدة إعلان
            </button>
            <p id="ad-msg" style="margin-top: 10px; font-weight: bold;"></p>
        </div>

        <!-- Other Tasks -->
        <div class="task-card">
            <h4><i class="fab fa-youtube" style="color: red;"></i> اشترك في القناة</h4>
            <p style="color: #777; font-size: 0.9rem;">اشترك في قناتنا على يوتيوب واربح 50 نقطة</p>
            <button class="btn-gold" style="padding: 10px; margin-top: 10px; font-size: 0.9rem;">تنفيذ المهمة</button>
        </div>

    </div>

    <!-- Bottom Nav -->
    <nav class="bottom-nav">
        <a href="/features/vip"><i class="fas fa-crown"></i><span>VIP</span></a>
        <a href="/features/wheel">
            <i class="fas fa-dharmachakra"></i>
            <span>العجلة</span>
        </a>
        <a href="/features/store">
            <i class="fas fa-store"></i>
            <span>المتجر</span>
        </a>
        <a href="/features/tasks" class="active">
            <i class="fas fa-tasks"></i>
            <span>المهام</span>
        </a>
        <a href="/features/profile">
            <i class="fas fa-user"></i>
            <span>حسابي</span>
        </a>
    </nav>

    <script>
        const watchBtn = document.getElementById('watch-ad-btn');
        const adMsg = document.getElementById('ad-msg');
        const adsCount = document.getElementById('ads-count');
        const cardsCount = document.getElementById('cards-count');
        const progressBar = document.getElementById('progress-bar');
        const dailyLimit = document.getElementById('daily-limit');

        watchBtn.addEventListener('click', async () => {
            watchBtn.disabled = true;
            watchBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> جاري المشاهدة...';
            adMsg.textContent = '';
            adMsg.style.color = '#fff';

            // Simulate 3 seconds ad
            setTimeout(async () => {
                try {
                    const response = await fetch('/features/watch-ad', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' }
                    });
                    const data = await response.json();

                    if (data.success) {
                        adsCount.textContent = data.adsWatched;
                        cardsCount.textContent = data.totalRedCards;
                        dailyLimit.textContent = data.cardsEarnedToday;
                        progressBar.style.width = (data.adsWatched / 7 * 100) + '%';

                        if (data.earnedCard) {
                            adMsg.textContent = '🎉 مبروك! حصلت على بطاقة حمراء!';
                            adMsg.style.color = '#00ff00';
                            // Quick visual celebration
                            watchBtn.style.background = '#00cc00';
                            setTimeout(() => {
                                watchBtn.style.background = ''; // reset
                            }, 1000);
                        } else {
                            adMsg.textContent = 'تم احتساب الإعلان ✅';
                        }
                    } else {
                        adMsg.textContent = data.message;
                        adMsg.style.color = '#ff4d4d';
                    }
                } catch (e) {
                    adMsg.textContent = 'حدث خطأ في الاتصال';
                } finally {
                    watchBtn.disabled = false;
                    watchBtn.innerHTML = '<i class="fas fa-play-circle"></i> مشاهدة إعلان';
                }
            }, 3000);
        });
    </script>
</body>

</html>