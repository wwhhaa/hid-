<!DOCTYPE html>
<html lang="ar" dir="rtl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تعيين كلمة مرور جديدة - التطبيق الآمن</title>
    <link rel="stylesheet" href="/css/login.css">
    <!-- Re-using login styles -->
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #1a1a1a;
            color: #fff;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .auth-container {
            background: #333;
            padding: 30px;
            border-radius: 10px;
            width: 300px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
        }

        .auth-container h2 {
            text-align: center;
            color: #f1c40f;
            margin-bottom: 20px;
        }

        .form-group {
            margin-bottom: 15px;
        }

        .form-group label {
            display: block;
            margin-bottom: 5px;
            color: #ccc;
        }

        .form-group input {
            width: 100%;
            padding: 10px;
            border: 1px solid #555;
            border-radius: 5px;
            background: #222;
            color: #fff;
            box-sizing: border-box;
        }

        .btn-submit {
            width: 100%;
            padding: 10px;
            background: #f1c40f;
            color: #111;
            border: none;
            border-radius: 5px;
            font-weight: bold;
            cursor: pointer;
            transition: 0.3s;
        }

        .btn-submit:hover {
            background: #d4ac0d;
        }
    </style>
</head>

<body>
    <div class="auth-container">
        <h2>كلمة مرور جديدة</h2>
        <% if(error) { %>
            <div style="color: #ff4d4d; text-align: center; margin-bottom: 15px;">
                <%= error %>
            </div>
            <% } %>
                <form action="/auth/reset-password/<%= token %>" method="POST">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <div class="form-group">
                        <label for="password">كلمة المرور الجديدة</label>
                        <input type="password" id="password" name="password" required minlength="6">
                    </div>
                    <button type="submit" class="btn-submit">حفظ كلمة المرور</button>
                </form>
                <p style="text-align: center; margin-top: 20px;">
                    <a href="/auth/login" style="color: #ccc; text-decoration: none;">إلغاء والعودة لتسجيل الدخول</a>
                </p>
    </div>
</body>

</html>